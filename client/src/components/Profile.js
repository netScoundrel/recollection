import './Profile.css';
import React, { Component } from 'react';
import Header from './Header';

export class Profile extends Component {
    render(){
        return(
            <React.Fragment>
                <Header />
            </React.Fragment>
        )
    }
}